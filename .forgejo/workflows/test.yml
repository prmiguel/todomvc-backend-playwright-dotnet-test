name: test
on:
  push:
    branches:
      - 'main'

jobs:
  build:
    name: build
    runs-on: docker-cli
    container:
      image: 'mcr.microsoft.com/dotnet/sdk:8.0'
    steps:
      - name: validate dotnet version
        run: |
          dotnet --version
      - run: ls -la
